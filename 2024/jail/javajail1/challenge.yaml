name: javajail1
author: flocto
description: |-
  Good luck getting anything to run.

  `{{nc}}`

flag:
  file: ./flag.txt

provide:
  - ./main.py

containers:
  main:
    build: .
    replicas: 1
    ports:
      - 5000
    securityContext:
      privileged: true

expose:
  main:
    - target: 5000
      tcp: 2103